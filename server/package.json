{
  "name": "mock-api",
  "version": "1.0.0",
  "private": true,
  "description": "Mock REST API backed by json-server",
  "scripts": {
    "start": "node server.js --port 4000",
    "mock": "json-server db.json --port 4000"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.15"
  }
}
